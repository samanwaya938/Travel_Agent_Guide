# Setup file for accessing local folders for making packages

from setuptools import setup, find_packages
setup(
    name='travel_agent_guide',
    version='0.1.0',
    description='A travel agent guide application',
    author='Your Name',
    packages=find_packages(),
    install_requires=[]
    )